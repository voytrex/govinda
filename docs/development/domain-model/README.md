## Domain Model (Internal)

This folder contains detailed, implementation-oriented domain model specifications.
It preserves the full entity definitions, relationships, and rules referenced by
the slimmed end-user documentation in `docs/domain`.
